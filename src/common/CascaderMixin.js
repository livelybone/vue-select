/**
 * @props {Array} options
 *    example: [{name: 'a', value:'1', children: [{name:'a1', value:'11', children:[...]}]}]
 * */
export default {
  props: {
    value: Array,
    options: Array,
  },
}
